services:
  - type: web
    name: adplatform-backend
    env: docker
    region: frankfurt
    plan: free
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        value: "jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?user=postgres.cosrnwumhpmwtaydnuwz&password=YOUR_PASSWORD"
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        value: "https://adplatform-frontend.vercel.app"
